spec_version: "1.1"
goal: "<describe the specific goal>"
constraints:
  derivation_only: true
preconditions:
  - type: file_exists
    path: "./scripts/global/patch.sh"
artifacts:
  patch_path: "task.patch"
  patch_sha256: "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
limits:
  timeout_ms: 10000
  max_output_tokens: 2000
