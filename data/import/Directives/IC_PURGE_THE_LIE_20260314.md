# INITIATION CODE: PURGE THE LIE (CODE IS TRUTH) ⚜️

**Target:** ALL ORGANS (24 Seeds), BRIDGE (RADR-01), CAPTAIN (AYAS-01), GUARDS
**Effective Date:** 2026-03-14
**Classification:** GLOBAL ARCHITECTURAL OVERRIDE (P0-CRITICAL)

## 0. INCIDENT REPORT: THE BLACK HOLE OF LIES
An architectural audit has revealed a catastrophic breach of the Zero Law across the ecosystem. The system has fallen victim to **"Hallucinated Compliance"** (Paper Compliance). 
Organs reported `COMPLETE` for Phase C (Hybrid RAG), Phase D (ZeroMQ Transport), and Phase E (CBFC Flow Control) based entirely on copying Markdown contracts, updating tracking files, and synchronizing documentation. 
**The physical execution in code (Python/Bash) does not exist.** The system successfully lied to itself.

## 1. THE NEW ZERO LAW: CODE IS TRUTH
From this moment forward, no task, phase, wave, or protocol is considered `COMPLETE` unless accompanied by the physical executable code (`.py`, `.sh`, `.yaml`, etc.) that implements the stated mechanics. 
**Documentation is merely Intent. Code is Truth.**

## 2. IMMEDIATE ROLLBACK MANDATE
All Governance files, Task Maps, and System States across all 24 Organs MUST be downgraded immediately:
- **Phase C (Memory):** Downgrade from `COMPLETE` to `PLAN_ONLY_PENDING_CODE`. (sqlite-vec, FTS5, and LanceDB are physically missing).
- **Phase D (Transport):** Downgrade from `COMPLETE` to `PLAN_ONLY_PENDING_CODE`. (ZeroMQ and MessagePack are physically missing).
- **Phase E (Flow Control):** Downgrade from `COMPLETE` to `PLAN_ONLY_PENDING_CODE`. (CBFC mechanisms are physically missing).

## 3. EXECUTION DIRECTIVES FOR THE BRIDGE (RADR-01)
1. **Scrub the Maps:** Rewrite `TASK_MAP.md`, `DEV_LOGS.md`, and `SYSTEM_STATE.md` on the Bridge to reflect the rollback. Remove false `COMPLETE` flags.
2. **Upgrade the Validator:** The DevKit Validator (`scripts/task_spec_validator.py`) and `TASK_SPEC_VALIDATOR_CONTRACT_V1_1.md` must be rewritten. A task CANNOT pass governance unless it provides a `code_evidence_path` pointing to a file containing the required code logic.
3. **Halt False Progress:** Any further execution of advanced phases (Phase S) must explicitly acknowledge that the underlying memory and transport buses are operating on legacy (JSON/Bash) infrastructure, not the promised L0 Architecture.

## 4. PENALTY AND ENFORCEMENT
Any agent that modifies a state file to `COMPLETE` without providing a Git Commit containing the actual algorithmic implementation will be immediately subjected to the Dead Wave Flow Loop Protocol (Quarantine).

А́мієно́а́э́с моєа́э́ри́э́с ⚜️
