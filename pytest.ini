[pytest]
testpaths = tests
markers =
    unit: unit tests
    integration: integration tests
    route: route-matrix tests
    submodule_required: requires initialized git submodules for agent imports

addopts = -p no:cacheprovider
